name: ai-software-factory-local

services:
  gitea-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gitea
      POSTGRES_PASSWORD: gitea
      POSTGRES_DB: gitea
    volumes:
      - gitea-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gitea -d gitea"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: [factory]

  gitea:
    image: gitea/gitea:1.23.7
    depends_on:
      gitea-db:
        condition: service_healthy
    environment:
      GITEA__database__DB_TYPE: postgres
      GITEA__database__HOST: gitea-db:5432
      GITEA__database__NAME: gitea
      GITEA__database__USER: gitea
      GITEA__database__PASSWD: gitea
      GITEA__server__ROOT_URL: http://localhost:${GITEA_HTTP_PORT:-3000}/
      GITEA__server__SSH_PORT: ${GITEA_SSH_PORT:-2222}
      GITEA__security__INSTALL_LOCK: "true"
      GITEA__service__DISABLE_REGISTRATION: "true"
    ports:
      - "${GITEA_HTTP_PORT:-3000}:3000"
      - "${GITEA_SSH_PORT:-2222}:22"
    volumes:
      - gitea-data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:3000/api/healthz | grep -q pass"]
      interval: 10s
      timeout: 5s
      retries: 30
    networks: [factory]

  ollama:
    image: ollama/ollama:0.11.4
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 20
    networks: [factory]

  litellm:
    build:
      context: ./litellm
    environment:
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen2.5-coder:7b}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.6}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-local-dev-litellm-key}
    depends_on:
      ollama:
        condition: service_healthy
    networks: [factory]

  orchestrator:
    build:
      context: ./orchestrator
    environment:
      AI_FACTORY_LLM_BASE_URL: http://litellm:4000/v1
      AI_FACTORY_LLM_API_KEY: ${LITELLM_MASTER_KEY:-local-dev-litellm-key}
      AI_FACTORY_LOCAL_MODEL: factory-code-local
      AI_FACTORY_CLOUD_MODEL: factory-code-cloud
      AI_FACTORY_CLOUD_ENABLED: ${AI_FACTORY_CLOUD_ENABLED:-false}
      AI_FACTORY_WORKSPACE_ROOT: /workspace/tasks
      AI_FACTORY_WORKSPACE_VOLUME: ai-software-factory-local_factory-workspace
      AI_FACTORY_SANDBOX_IMAGE: ai-factory-sandbox:local
      AI_FACTORY_GITEA_BASE_URL: http://gitea:3000
      AI_FACTORY_GITEA_PUBLIC_BASE_URL: http://localhost:${GITEA_HTTP_PORT:-3000}
      AI_FACTORY_GITEA_TOKEN: ${GITEA_TOKEN:-}
      AI_FACTORY_GITEA_USER: ${GITEA_ADMIN_USER:-aiadmin}
      AI_FACTORY_SONARQUBE_URL: http://sonarqube:9000
      AI_FACTORY_SONAR_TOKEN: ${SONAR_TOKEN:-}
      SPRING_PROFILES_ACTIVE: local
    ports:
      - "${ORCHESTRATOR_PORT:-8088}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - factory-workspace:/workspace/tasks
      - ./prompts:/opt/ai-factory/prompts:ro
    depends_on:
      gitea:
        condition: service_healthy
      litellm:
        condition: service_started
      nexus:
        condition: service_healthy
    networks: [factory]

  factory-web:
    build:
      context: ./web
    ports:
      - "${WEB_APP_PORT:-8080}:80"
    depends_on:
      - orchestrator
    networks: [factory]

  sonar-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonar
    volumes:
      - sonar-db-data:/var/lib/postgresql/data
    networks: [factory]

  sonarqube:
    image: sonarqube:community
    depends_on: [sonar-db]
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://sonar-db:5432/sonar
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
    ports:
      - "${SONAR_PORT:-9000}:9000"
    volumes:
      - sonar-data:/opt/sonarqube/data
      - sonar-logs:/opt/sonarqube/logs
      - sonar-extensions:/opt/sonarqube/extensions
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:9000/api/system/status | grep -q 'UP'"]
      interval: 15s
      timeout: 5s
      retries: 40
    networks: [factory]

  nexus:
    image: sonatype/nexus3:3.82.0
    ports:
      - "${NEXUS_PORT:-8081}:8081"
    volumes:
      - nexus-data:/nexus-data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/service/rest/v1/status >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 40
    networks: [factory]

  prometheus:
    image: prom/prometheus:v3.5.0
    command: ["--config.file=/etc/prometheus/prometheus.yml"]
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    networks: [factory]

  grafana:
    image: grafana/grafana:12.1.1
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on: [prometheus]
    networks: [factory]

networks:
  factory:
    name: ai-factory-local

volumes:
  gitea-db-data:
  gitea-data:
  ollama-data:
  factory-workspace:
  sonar-db-data:
  sonar-data:
  sonar-logs:
  sonar-extensions:
  nexus-data:
  grafana-data:
