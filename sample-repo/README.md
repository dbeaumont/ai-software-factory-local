# Customer API demo

Minimal Spring Boot repository used by the AI Software Factory prototype.

Current API:
- `GET /customers`

Good demo requirement:
> Add `GET /customers/{id}` returning 404 when the customer does not exist, and add tests.
