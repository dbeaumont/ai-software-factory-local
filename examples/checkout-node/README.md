# Checkout Node demo

Small dependency-free Node.js checkout domain used by the MCP context shadow campaign.

Current behavior:

- totals positive line items;
- rejects malformed or negative prices;
- uses the built-in Node test runner.
